# AI Fairness Audit System

### Backend Setup

cd backend
python -m venv venv
venv\Scripts\activate   # on Windows
pip install -r requirements.txt
python app.py

### Frontend Setup
cd frontend
npm install
npm start
